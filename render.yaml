databases:
  - name: saas-desk-ai-db
    plan: free

services:
  - type: web
    name: saas-desk-ai-web
    plan: free
    runtime: docker
    dockerCommand: python -m config.render_start
    healthCheckPath: /health/
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: saas-desk-ai-db
          property: connectionString
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "false"
      - key: ANALYSIS_PROVIDER
        value: openrouter
      - key: CELERY_TASK_ALWAYS_EAGER
        value: "true"
      - key: OPENROUTER_MODEL
        value: openrouter/free
      - key: OPENROUTER_TIMEOUT_SECONDS
        value: "20"
      - key: OPENROUTER_API_KEY
        sync: false
      - key: DEMO_ANALYST_PASSWORD
        value: demo-password
